import { ChangeVowelsPipe } from './change-vowels.pipe';

describe('ChangeVowelsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeVowelsPipe();
    expect(pipe).toBeTruthy();
  });
});
